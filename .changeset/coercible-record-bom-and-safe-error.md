---
"freee-mcp": patch
---

`freee_api_*` の query/body が JSON 文字列で届いた場合に、先頭の UTF-8 BOM が含まれていれば BOM 検出専用のエラーメッセージを返すようにした（OS によって挙動が分かれないよう自動除去はしない）。それ以外のパース不能ケースのエラーメッセージはペイロード本体を含めず文字列長のみを含む形にし、業務データが応答やログに漏れないようにした。
