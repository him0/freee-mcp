# Development: docker compose up redis
# Observability: docker compose up redis otel-collector jaeger prometheus grafana
# Production:   docker compose up
services:
  freee-mcp:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      OTEL_ENABLED: "true"
      OTEL_SERVICE_NAME: freee-mcp
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2"
    restart: unless-stopped

  freee-sign-mcp:
    build:
      context: .
      dockerfile: Dockerfile.sign
    ports:
      - "3002:3002"
    env_file: .env
    environment:
      # 共有 Redis は freee-mcp 本体と `freee-sign-mcp:*` prefix + Valkey RBAC で分離する方針のため
      # DB 番号での論理分離は行わない（将来の cluster mode 移行を阻害しないため）
      SIGN_REDIS_URL: redis://redis:6379
      OTEL_ENABLED: "true"
      OTEL_SERVICE_NAME: freee-sign-mcp
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4318
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    # allkeys-lru 下では freee-mcp 本体と Sign の key が相互に巻き添え eviction され得るため、
    # 本番障害の顕在化を避けて noeviction で OOM を書き込みエラーとして fail-fast させる運用にする
    command: redis-server --maxmemory 256mb --maxmemory-policy noeviction
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 3

  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    ports:
      - "4318:4318"
    volumes:
      - ./otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml
    restart: unless-stopped

  jaeger:
    image: jaegertracing/all-in-one:latest
    ports:
      - "16686:16686"
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=7d"
    volumes:
      - ./prometheus.yaml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    restart: unless-stopped

  grafana:
    image: grafana/grafana-oss:latest
    ports:
      - "3001:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
    volumes:
      - grafana-data:/var/lib/grafana
    restart: unless-stopped

volumes:
  redis-data:
  prometheus-data:
  grafana-data:
